# Conformance vectors

A **vector** is a file with a known input and the exact output the code must
produce for it. `src/iltero_schemas/vectors/` holds them; they ship in the
wheel as `iltero_schemas.vectors.VECTORS`. Anyone using this package — the
Iltero CLI, Iltero Compass, or a third party — can run them against the
version they installed. If every file is reproduced, the two sides agree on
the contract. If not, that build must fail.

| Folder | What it holds | How it is made |
| --- | --- | --- |
| `canonical/` | `values.json`: sample values. `cases.json`: their canonical bytes and fingerprints | `scripts/refresh_vectors.py` |
| `assertions/` | Small assertions written only to exercise the language (truth tables, each comparison, nesting) | by hand |
| `compiler/` | For every shipped and vector assertion: the AST as JSON (`.ast.json`), its fingerprint (`.digest`), the compiled program (`.rego`) and its fingerprint (`.rego.digest`). Also `COMPILER_VERSION` and `RUNTIME.digest`, the fingerprint of the shared runtime | `scripts/refresh_vectors.py` |
| `invalid/` | Documents that must be rejected, with `expected.json` naming the key and the message of the first problem | by hand |
| `evaluation/` | `cases.json`: an assertion, an input, and the status, reason, unknown value and (for some) observations OPA must return | by hand |
| `contexts/` | One complete assurance context per profile (`plan_resource.json`: a production RDS change at the `plan` stage) and `digests.json`, the `input_digest` of each. The tests also run the scenario's assertions against it | the contexts by hand; `digests.json` by `scripts/refresh_vectors.py` |
| `events/` | A complete assurance event (`plan_pass.json`: the verdict for the context above) and `digests.json` | the event by hand; `digests.json` by `scripts/refresh_vectors.py` |
| `opa/` | `not_allowed.txt`: every built-in function of the pinned OPA release that the allowlist leaves out | `scripts/refresh_capabilities.py` |

`tests/test_vectors.py` reproduces all of them.

## On this page

- [Running the tests that need OPA](#running-the-tests-that-need-opa)
- [Changing the parser, the compiler or the runtime](#changing-the-parser-the-compiler-or-the-runtime)
- [Changing the list of allowed OPA functions](#changing-the-list-of-allowed-opa-functions)

## Running the tests that need OPA

The evaluation cases, and the checks that each compiled program is accepted
by OPA (`opa check --strict --capabilities`, `opa fmt`, `opa build`), need the
pinned OPA binary. Point `ILTERO_OPA_PATH` at it, or put `opa` on `PATH`.
The tests hash the binary first: a different release fails the tests; no
binary at all skips them. In CI `ILTERO_OPA_REQUIRED=1` is set, so a missing
binary fails instead of skipping.

To get the binary, run `python scripts/fetch_opa.py DIR`. It downloads the
release for your machine, checks the download against `PIN.json`, and only
then keeps it. CI does the same.

## Changing the parser, the compiler or the runtime

1. Make the change, with its tests.
2. If any generated byte changes, bump `COMPILER_VERSION` in
   `iltero_schemas/compiler/emit.py`. If the AST's JSON form changes, bump
   `AST_VERSION` in `iltero_schemas/ast/nodes.py`. Both are breaking changes
   of the contract (see [versioning](02-versioning.md)).
3. Run `pdm run python scripts/refresh_vectors.py` and read the diff of the
   vectors as carefully as the code. It is what every user of the package
   will reproduce.

The script refuses to run when `runtime.rego` changed but `COMPILER_VERSION`
did not: every recorded program fingerprint would change while the version
that explains it stayed the same. `--check` (part of the test suite) fails
when the vectors and the code disagree.

## Changing the list of allowed OPA functions

`src/iltero_schemas/opa/capabilities.json` names the functions a program may
call. The names are the decision. When the OPA pin moves, run
`python scripts/refresh_capabilities.py <opa>` with the newly pinned binary.
The script first checks the binary's fingerprint, then rewrites the function
declarations from that binary, keeps the same names, and stops if a name has
disappeared. It also rewrites `vectors/opa/not_allowed.txt`, so every function
the new release added shows up in the diff and has to be accepted or rejected
on purpose. `--check` (part of the test suite) fails when either file does not
match the pinned binary.
