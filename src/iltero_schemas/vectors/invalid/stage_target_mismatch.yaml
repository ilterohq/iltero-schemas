apiVersion: iltero.io/v1
kind: TechnicalAssertion
metadata:
  id: VEC.INVALID.CASE
  version: "1.0.0"
  title: An invalid document
spec:
  stage: plan
  target: {kind: change}
  assert:
    path: change.digest
    equal: x
